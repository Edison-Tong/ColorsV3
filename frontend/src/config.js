// Where the ColorsV3 backend is running.
//
// - iOS simulator / web browser on the same Mac:  http://localhost:4000
// - Android emulator:                             http://10.0.2.2:4000
// - Physical phone via Expo Go:                   http://<your-computer-LAN-IP>:4000
//   (find it with `ipconfig getifaddr en0` on macOS). The phone and computer must
//   be on the same Wi-Fi network.
//
// Edit this one line to match your setup.
export const BACKEND_URL = "http://localhost:4000";
